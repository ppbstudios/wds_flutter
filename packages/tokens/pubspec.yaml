name: wds_tokens
version: 0.1.0
publish_to: "none"
description: WINC Design System tokens package
environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
