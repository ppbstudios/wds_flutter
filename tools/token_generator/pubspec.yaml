name: token_generator
version: 0.1.0
publish_to: "none"
description: WDS JSON -> Dart 코드 생성기
environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  path: ^1.9.0
  args: ^2.5.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0

executables:
  wds_tokens: bin/main.dart
